<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="widget_Dialog_Tip" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowSoftInputMode">adjustPan</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 模糊 -->
    </style>


    <!--底部弹出的动画-->
    <style name="widget_anim_slide_share_from_bottom">
        <item name="android:windowEnterAnimation">@anim/widget_slide_share_from_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/widget_slide_share_from_bottom_out</item>
    </style>
</resources>